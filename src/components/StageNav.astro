---
import { STAGES, STAGE_LABELS } from '../lib/constants';
import { withBase } from '../lib/url';
---

<nav aria-label="Stages" class="tabs tabs-box tabs-sm flex-wrap">
  {STAGES.map((stage) => (
    <a class="tab" href={withBase(`/stage/${stage}/`)}>{STAGE_LABELS[stage]}</a>
  ))}
</nav>
